<h2>Gallery</h2>
<h3>Title: {{ galleryFromDB.galleryTitle }}</h3>
<h4>Description: {{ galleryFromDB.galleryDescription }}</h4>
<h5>Theme: {{ galleryFromDB.galleryTheme }}</h5>

<div>
  {{#each galleryFromDB.image}}
  Title: {{title}}
  
  <img src="{{title}}"
  {{/each}}
</div>



{{!-- {{#each validImages}}
<div>
  <img src="{{" alt="">
{{#if editable}}
<form action="/gallery/delete/{{this._id}}" method="POST">
<button>X</button>
</form>
{{/if}}
  {{#if editable}}
<form action="/gallery/edit/{{this._id}}">
  <input type="text" value="{{galleryImage.title}}">
  <button>Update</button>
</form>
  {{else}}
  <h3>{{galleryImage.title}}</h3>
  {{/if}}
</div>
{{/each}} --}}

  {{!-- //filter - check all images in gallery, if any are undefined remove from view before displaying
  {{!-- display the array of validImages (separate array of images)
  --}}

  